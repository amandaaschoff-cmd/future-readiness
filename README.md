# Future Readiness (Vite + React + Tailwind)

Minimal starter that deploys cleanly on Vercel (no Next.js).

## Scripts
- `npm run dev` — local dev
- `npm run build` — production build
- `npm run preview` — preview built app

Output directory: `dist/`
